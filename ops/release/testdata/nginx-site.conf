server {
    listen 80;
    server_name _;

    # MadAPI native CPA Codex routes begin
    location ^~ /codex/v1/ {
        return 418;
    }
    # MadAPI native CPA Codex routes end

    location / {
        proxy_pass http://new-api:3000;
    }
}
