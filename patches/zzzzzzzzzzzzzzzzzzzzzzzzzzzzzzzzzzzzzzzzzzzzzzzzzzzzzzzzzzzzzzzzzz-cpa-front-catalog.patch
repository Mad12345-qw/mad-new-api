diff --git a/controller/internal_codex_catalog.go b/controller/internal_codex_catalog.go
new file mode 100644
index 00000000..7bbebce5
--- /dev/null
+++ b/controller/internal_codex_catalog.go
@@ -0,0 +1,46 @@
+package controller
+
+import (
+	"crypto/subtle"
+	"os"
+	"sort"
+	"strings"
+
+	"github.com/QuantumNous/new-api/dto"
+	"github.com/QuantumNous/new-api/model"
+	"github.com/gin-gonic/gin"
+)
+
+const madAPIInternalCatalogTokenEnv = "MADAPI_INTERNAL_CATALOG_TOKEN"
+
+func madAPIInternalCatalogAuthorized(value string) bool {
+	expected := strings.TrimSpace(os.Getenv(madAPIInternalCatalogTokenEnv))
+	provided := strings.TrimSpace(value)
+	return expected != "" && len(expected) == len(provided) && subtle.ConstantTimeCompare([]byte(expected), []byte(provided)) == 1
+}
+
+// InternalCodexCatalog returns enabled text models to the private CPA front proxies.
+func InternalCodexCatalog(c *gin.Context) {
+	if !madAPIInternalCatalogAuthorized(c.GetHeader("X-MadAPI-Catalog-Token")) {
+		c.Status(404)
+		return
+	}
+
+	enabled := make(map[string]struct{})
+	for _, modelName := range model.GetEnabledModels() {
+		enabled[modelName] = struct{}{}
+	}
+
+	models := make([]dto.OpenAIModels, 0)
+	for _, pricing := range model.GetPricing() {
+		if pricing.QuotaType != 0 {
+			continue
+		}
+		if _, ok := enabled[pricing.ModelName]; !ok {
+			continue
+		}
+		models = append(models, buildOpenAIModel(pricing.ModelName, nil))
+	}
+	sort.Slice(models, func(i, j int) bool { return models[i].Id < models[j].Id })
+	c.JSON(200, gin.H{"object": "list", "data": models})
+}
diff --git a/controller/internal_codex_catalog_test.go b/controller/internal_codex_catalog_test.go
new file mode 100644
index 00000000..c0bba111
--- /dev/null
+++ b/controller/internal_codex_catalog_test.go
@@ -0,0 +1,22 @@
+package controller
+
+import (
+	"os"
+	"testing"
+)
+
+func TestMadAPIInternalCatalogAuthorized(t *testing.T) {
+	t.Setenv(madAPIInternalCatalogTokenEnv, "catalog-secret")
+	if !madAPIInternalCatalogAuthorized("catalog-secret") {
+		t.Fatal("expected the deployment catalog token to be accepted")
+	}
+	if madAPIInternalCatalogAuthorized("wrong-secret") {
+		t.Fatal("expected a different catalog token to be rejected")
+	}
+	if err := os.Unsetenv(madAPIInternalCatalogTokenEnv); err != nil {
+		t.Fatal(err)
+	}
+	if madAPIInternalCatalogAuthorized("catalog-secret") {
+		t.Fatal("expected an unset catalog token to reject access")
+	}
+}
diff --git a/router/relay-router.go b/router/relay-router.go
index 00000000..00000000 100644
--- a/router/relay-router.go
+++ b/router/relay-router.go
@@ -37,6 +37,11 @@ func SetRelayRouter(router *gin.Engine) {
 		codexCockpitRouter.POST("/responses/compact", controller.CodexResponsesCompact)
 	}
 
+	internalCodexRouter := router.Group("/internal/codex")
+	{
+		internalCodexRouter.GET("/models", controller.InternalCodexCatalog)
+	}
+
 	// https://platform.openai.com/docs/api-reference/introduction
 	modelsRouter := router.Group("/v1/models")
 	modelsRouter.Use(middleware.RouteTag("relay"))
