diff --git a/controller/codex_catalog.go b/controller/codex_catalog.go
index 878b76a..afab91f 100644
--- a/controller/codex_catalog.go
+++ b/controller/codex_catalog.go
@@ -54,16 +54,18 @@ var (
 )
 
 var codexValidatedImageInputModels = map[string]bool{
-	"claude-fable-5":   true,
-	"claude-opus-4-8":  true,
-	"claude-opus-5":    true,
-	"gemini-3.6-flash": true,
-	"gpt-5.5":          true,
-	"gpt-5.6-luna":     true,
-	"gpt-5.6-sol":      true,
-	"gpt-5.6-terra":    true,
-	"grok-4.5":         true,
-	"kimi-k3":          true,
+	"claude-fable-5":    true,
+	"claude-opus-4-8":   true,
+	"claude-opus-5":     true,
+	"gemini-3.6-flash":  true,
+	"gpt-5.5":           true,
+	"gpt-5.6-luna":      true,
+	"gpt-5.6-sol":       true,
+	"gpt-5.6-sol-pro":   true,
+	"gpt-5.6-terra":     true,
+	"gpt-5.6-terra-pro": true,
+	"grok-4.5":          true,
+	"kimi-k3":           true,
 }
 
 var codexNativeSearchModels = map[string]bool{
@@ -76,17 +78,21 @@ var codexNativeSearchModels = map[string]bool{
 	"gpt-5.5":             true,
 	"gpt-5.6-luna":        true,
 	"gpt-5.6-sol":         true,
+	"gpt-5.6-sol-pro":     true,
 	"gpt-5.6-terra":       true,
+	"gpt-5.6-terra-pro":   true,
 	"grok-4.5":            true,
 	"kimi-k3":             true,
 	"qwen3.8-max-preview": true,
 }
 
 var codexNativeVerbosityModels = map[string]bool{
-	"gpt-5.5":       true,
-	"gpt-5.6-luna":  true,
-	"gpt-5.6-sol":   true,
-	"gpt-5.6-terra": true,
+	"gpt-5.5":           true,
+	"gpt-5.6-luna":      true,
+	"gpt-5.6-sol":       true,
+	"gpt-5.6-sol-pro":   true,
+	"gpt-5.6-terra":     true,
+	"gpt-5.6-terra-pro": true,
 }
 
 // These aliases are absent from CPA's pinned static snapshot but are documented
@@ -128,6 +134,24 @@ var codexOfficialCapabilityOverrides = map[string]codexCPAModelInfo{
 	},
 }
 
+type codexCapabilityAlias struct {
+	BaseID      string
+	DisplayName string
+}
+
+var codexCapabilityAliases = map[string]codexCapabilityAlias{
+	"gpt-5.6-sol-pro":   {BaseID: "gpt-5.6-sol", DisplayName: "GPT 5.6 Sol Pro"},
+	"gpt-5.6-terra-pro": {BaseID: "gpt-5.6-terra", DisplayName: "GPT 5.6 Terra Pro"},
+}
+
+func codexModelInfoForRequestedID(id string, info codexCPAModelInfo) *codexCPAModelInfo {
+	info.ID = id
+	if alias, ok := codexCapabilityAliases[id]; ok {
+		info.DisplayName = alias.DisplayName
+	}
+	return &info
+}
+
 func loadCodexCPACatalog() map[string][]codexCPAModelInfo {
 	codexCPACatalogOnce.Do(func() {
 		codexCPACatalog = make(map[string][]codexCPAModelInfo)
@@ -150,10 +174,14 @@ func loadCodexCPACatalog() map[string][]codexCPAModelInfo {
 
 func codexCatalogModel(candidate dto.OpenAIModels) *codexCPAModelInfo {
 	id := strings.TrimSpace(candidate.Id)
-	if override, ok := codexOfficialCapabilityOverrides[id]; ok {
-		return &override
+	lookupID := id
+	if alias, ok := codexCapabilityAliases[id]; ok {
+		lookupID = alias.BaseID
+	}
+	if override, ok := codexOfficialCapabilityOverrides[lookupID]; ok {
+		return codexModelInfoForRequestedID(id, override)
 	}
-	matches := loadCodexCPACatalog()[id]
+	matches := loadCodexCPACatalog()[lookupID]
 	if len(matches) == 0 {
 		return nil
 	}
@@ -162,7 +190,7 @@ func codexCatalogModel(candidate dto.OpenAIModels) *codexCPAModelInfo {
 		infoOwner := strings.ToLower(strings.TrimSpace(matches[index].OwnedBy))
 		infoType := strings.ToLower(strings.TrimSpace(matches[index].Type))
 		if owner != "" && (owner == infoOwner || owner == infoType || strings.Contains(owner, infoType)) {
-			return &matches[index]
+			return codexModelInfoForRequestedID(id, matches[index])
 		}
 	}
 	best := 0
@@ -179,7 +207,7 @@ func codexCatalogModel(candidate dto.OpenAIModels) *codexCPAModelInfo {
 			best = index
 		}
 	}
-	return &matches[best]
+	return codexModelInfoForRequestedID(id, matches[best])
 }
 
 func codexReasoningDescription(level string) string {
diff --git a/controller/codex_cockpit.go b/controller/codex_cockpit.go
index 9dafabf..dafcc28 100644
--- a/controller/codex_cockpit.go
+++ b/controller/codex_cockpit.go
@@ -34,8 +34,8 @@ var codexCockpitModelTargets = []struct {
 	{UpstreamModel: "gpt-5.6-terra", ClientModel: "gpt-5.6-terra"},
 	{UpstreamModel: "gpt-5.6-luna", ClientModel: "gpt-5.6-luna"},
 	{UpstreamModel: "grok-4.5", ClientModel: "gpt-5.4-mini"},
-	{UpstreamModel: "kimi-k3", ClientModel: "gpt-5.3-codex"},
-	{UpstreamModel: "deepseek-v4-flash", ClientModel: "gpt-5.2"},
+	{UpstreamModel: "gpt-5.6-sol-pro", ClientModel: "gpt-5.3-codex"},
+	{UpstreamModel: "gpt-5.6-terra-pro", ClientModel: "gpt-5.2"},
 }
 
 func isCodexCockpitModelShell(model string) bool {
diff --git a/controller/codex_compat.go b/controller/codex_compat.go
index c2b1082..2cae233 100644
--- a/controller/codex_compat.go
+++ b/controller/codex_compat.go
@@ -114,6 +114,9 @@ func codexAvailableModel(candidate dto.OpenAIModels) map[string]any {
 		"display_name": candidate.Id,
 		"description":  "Available through MadAPI: " + candidate.Id,
 	}
+	if alias, ok := codexCapabilityAliases[candidate.Id]; ok {
+		model["template_id"] = alias.BaseID
+	}
 	if info := codexCatalogModel(candidate); info != nil {
 		if value := strings.TrimSpace(info.DisplayName); value != "" {
 			model["display_name"] = value
diff --git a/controller/codex_compat_test.go b/controller/codex_compat_test.go
index 0f4f4f4..31ec974 100644
--- a/controller/codex_compat_test.go
+++ b/controller/codex_compat_test.go
@@ -192,12 +192,12 @@ func TestCodexListModelsReflectsNewTextModelsWithoutServerRestart(t *testing.T)
 			_, _ = io.WriteString(w, `{"object":"list","data":[{"id":"existing-text-model","object":"model","supported_endpoint_types":["openai-response"]}]}`)
 			return
 		}
-		_, _ = io.WriteString(w, `{"object":"list","data":[{"id":"existing-text-model","object":"model","supported_endpoint_types":["openai-response"]},{"id":"new-text-model","object":"model","supported_endpoint_types":["anthropic"]}]}`)
-	}), "existing-text-model", "new-text-model")
+		_, _ = io.WriteString(w, `{"object":"list","data":[{"id":"existing-text-model","object":"model","supported_endpoint_types":["openai-response"]},{"id":"gpt-5.6-sol-pro","object":"model","supported_endpoint_types":["openai-response"]},{"id":"gpt-5.6-terra-pro","object":"model","supported_endpoint_types":["openai-response"]}]}`)
+	}), "existing-text-model", "gpt-5.6-sol-pro", "gpt-5.6-terra-pro")
 
 	wantSlugs := [][]string{
 		{"existing-text-model"},
-		{"existing-text-model", "new-text-model"},
+		{"existing-text-model", "gpt-5.6-sol-pro", "gpt-5.6-terra-pro"},
 	}
 	for index, want := range wantSlugs {
 		recorder := httptest.NewRecorder()
@@ -221,13 +221,54 @@ func TestCodexListModelsReflectsNewTextModelsWithoutServerRestart(t *testing.T)
 	require.Equal(t, int32(2), calls.Load())
 }
 
+func TestCodexListModelsMakesProModelsExactCapabilityAliases(t *testing.T) {
+	gin.SetMode(gin.TestMode)
+	modelNames := []string{"gpt-5.6-sol", "gpt-5.6-sol-pro", "gpt-5.6-terra", "gpt-5.6-terra-pro"}
+	data := make([]string, 0, len(modelNames))
+	for _, modelName := range modelNames {
+		data = append(data, fmt.Sprintf(`{"id":%q,"object":"model","owned_by":"openai","supported_endpoint_types":["openai-response"]}`, modelName))
+	}
+	withCodexCompatibilityTestServer(t, http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
+		w.Header().Set("Content-Type", "application/json")
+		_, _ = io.WriteString(w, `{"object":"list","data":[`+strings.Join(data, ",")+`]}`)
+	}), modelNames...)
+
+	recorder := httptest.NewRecorder()
+	c, _ := gin.CreateTestContext(recorder)
+	c.Request = httptest.NewRequest(http.MethodGet, "/codex/v1/models", nil)
+	c.Request.Header.Set("Authorization", "Bearer test-token")
+	CodexListModels(c)
+	require.Equal(t, http.StatusOK, recorder.Code)
+
+	var payload codexModelsResponse
+	require.NoError(t, json.Unmarshal(recorder.Body.Bytes(), &payload))
+	bySlug := make(map[string]map[string]any, len(payload.Models))
+	for _, entry := range payload.Models {
+		bySlug[entry["slug"].(string)] = entry
+	}
+	for _, pair := range [][2]string{{"gpt-5.6-sol", "gpt-5.6-sol-pro"}, {"gpt-5.6-terra", "gpt-5.6-terra-pro"}} {
+		base := bySlug[pair[0]]
+		pro := bySlug[pair[1]]
+		require.NotNil(t, base)
+		require.NotNil(t, pro)
+		for _, field := range []string{
+			"context_window", "max_context_window", "max_completion_tokens",
+			"input_modalities", "supports_image_detail_original", "support_verbosity",
+			"default_verbosity", "supported_reasoning_levels", "default_reasoning_level",
+			"supports_search_tool", "web_search_tool_type", "apply_patch_tool_type",
+		} {
+			require.Equal(t, base[field], pro[field], "%s differs for %s", field, pair[1])
+		}
+	}
+}
+
 func TestCodexCockpitListModelsPublishesTheStableAPIKeySelection(t *testing.T) {
 	gin.SetMode(gin.TestMode)
 	modelNames := []string{
 		"gpt-5.6-sol", "gpt-5.6-terra", "gpt-5.6-luna", "gpt-5.5",
 		"claude-fable-5", "claude-opus-4-8", "claude-opus-5", "claude-sonnet-5",
 		"deepseek-v4-flash", "deepseek-v4-pro", "gemini-3.6-flash", "glm-5-2",
-		"grok-4.5", "kimi-k3", "qwen3.8-max-preview",
+		"grok-4.5", "kimi-k3", "qwen3.8-max-preview", "gpt-5.6-sol-pro", "gpt-5.6-terra-pro",
 	}
 	data := make([]string, 0, len(modelNames))
 	for _, modelName := range modelNames {
@@ -262,10 +303,14 @@ func TestCodexCockpitListModelsPublishesTheStableAPIKeySelection(t *testing.T) {
 	require.Equal(t, "gpt-5.6-terra", clientModelByUpstream["gpt-5.6-terra"])
 	require.Equal(t, "gpt-5.6-luna", clientModelByUpstream["gpt-5.6-luna"])
 	require.Equal(t, "gpt-5.4-mini", clientModelByUpstream["grok-4.5"])
-	require.Equal(t, "gpt-5.3-codex", clientModelByUpstream["kimi-k3"])
-	require.Equal(t, "gpt-5.2", clientModelByUpstream["deepseek-v4-flash"])
+	require.Equal(t, "gpt-5.3-codex", clientModelByUpstream["gpt-5.6-sol-pro"])
+	require.Equal(t, "gpt-5.2", clientModelByUpstream["gpt-5.6-terra-pro"])
 	_, containsOverflow := clientModelByUpstream["deepseek-v4-pro"]
 	require.False(t, containsOverflow)
+	_, containsReplacedKimi := clientModelByUpstream["kimi-k3"]
+	require.False(t, containsReplacedKimi)
+	_, containsReplacedDeepSeek := clientModelByUpstream["deepseek-v4-flash"]
+	require.False(t, containsReplacedDeepSeek)
 }
 
 func TestCodexCockpitListModelsRefreshesSelectedAvailabilityWithoutServerRestart(t *testing.T) {
diff --git a/service/codexclientmodels/models.go b/service/codexclientmodels/models.go
index 709f06e..a524354 100644
--- a/service/codexclientmodels/models.go
+++ b/service/codexclientmodels/models.go
@@ -74,9 +74,17 @@ func buildCodexClientModels(models []map[string]any, providersForModel Providers
 			continue
 		}
 
-		if template, ok := templates[id]; ok {
+		templateID := id
+		if alias := strings.TrimSpace(stringModelValue(model, "template_id")); alias != "" {
+			templateID = alias
+		}
+		if template, ok := templates[templateID]; ok {
 			entry := cloneCodexClientModelMap(template)
-			applyCodexClientDisplayName(entry, model)
+			if templateID == id {
+				applyCodexClientDisplayName(entry, model)
+			} else {
+				applyCodexClientTemplateAliasMetadata(entry, id, model)
+			}
 			applyCodexClientSearchToolSupport(entry, id, true, providersForModel, supportsSearch)
 			sanitizeCodexClientReasoningMetadata(entry)
 			applyCodexClientVisibilityOverride(entry, id)
@@ -201,6 +209,14 @@ func applyCodexClientDisplayName(entry map[string]any, model map[string]any) {
 	}
 }
 
+func applyCodexClientTemplateAliasMetadata(entry map[string]any, id string, model map[string]any) {
+	entry["slug"] = id
+	applyCodexClientDisplayName(entry, model)
+	if description := stringModelValue(model, "description"); description != "" {
+		entry["description"] = description
+	}
+}
+
 func applyCodexClientSearchToolSupport(entry map[string]any, id string, templateModel bool, providersForModel ProvidersForModelFunc, searchSupport SearchSupportFunc) {
 	if searchSupport != nil {
 		entry["supports_search_tool"] = searchSupport(id)


