diff --git a/internal/translator/claude/openai/responses/claude_openai-responses_response.go b/internal/translator/claude/openai/responses/claude_openai-responses_response.go
index e6de8c7..ebb4660 100644
--- a/internal/translator/claude/openai/responses/claude_openai-responses_response.go
+++ b/internal/translator/claude/openai/responses/claude_openai-responses_response.go
@@ -31,6 +31,8 @@ type claudeToResponsesState struct {
 	FuncCallIDs       map[int]string // Claude block index -> call id
 	FuncOutputIndices map[int]int    // Claude block index -> Responses output index
 	FuncCustom        map[int]bool   // Claude block index -> original Responses custom tool
+	SearchCalls       map[int]*claudeResponsesSearchCall
+	SearchCallByID    map[string]int
 	// message text aggregation
 	TextBuf            strings.Builder
 	CurrentTextBuf     strings.Builder
@@ -108,6 +110,13 @@ func pickRequestJSON(originalRequestRawJSON, requestRawJSON []byte) []byte {
 	return nil
 }
 
+type claudeResponsesSearchCall struct {
+	ID          string
+	OutputIndex int
+	Query       strings.Builder
+	Completed   bool
+}
+
 func claudeResponsesCustomToolNames(requestRawJSON []byte) map[string]struct{} {
 	names := make(map[string]struct{})
 	var collect func(gjson.Result, string)
@@ -274,6 +283,8 @@ func ConvertClaudeResponseToOpenAIResponses(ctx context.Context, modelName strin
 			FuncCallIDs:        make(map[int]string),
 			FuncOutputIndices:  make(map[int]int),
 			FuncCustom:         make(map[int]bool),
+			SearchCalls:        make(map[int]*claudeResponsesSearchCall),
+			SearchCallByID:     make(map[string]int),
 		}
 	}
 	st := (*param).(*claudeToResponsesState)
@@ -392,6 +403,47 @@ func ConvertClaudeResponseToOpenAIResponses(ctx context.Context, modelName strin
 			// record function metadata for aggregation
 			st.FuncCallIDs[idx] = st.CurrentFCID
 			st.FuncNames[idx] = name
+		} else if typ == "server_tool_use" && cb.Get("name").String() == "web_search" {
+			callID := cb.Get("id").String()
+			search := &claudeResponsesSearchCall{
+				ID:          callID,
+				OutputIndex: st.allocateOutputIndex(),
+			}
+			st.SearchCalls[idx] = search
+			st.SearchCallByID[callID] = idx
+
+			item := []byte(`{"type":"response.output_item.added","sequence_number":0,"output_index":0,"item":{"id":"","type":"web_search_call","status":"in_progress"}}`)
+			item, _ = sjson.SetBytes(item, "sequence_number", nextSeq())
+			item, _ = sjson.SetBytes(item, "output_index", search.OutputIndex)
+			item, _ = sjson.SetBytes(item, "item.id", fmt.Sprintf("ws_%s", callID))
+			out = append(out, emitEvent("response.output_item.added", item))
+
+			inProgress := []byte(`{"type":"response.web_search_call.in_progress","sequence_number":0,"item_id":"","output_index":0}`)
+			inProgress, _ = sjson.SetBytes(inProgress, "sequence_number", nextSeq())
+			inProgress, _ = sjson.SetBytes(inProgress, "item_id", fmt.Sprintf("ws_%s", callID))
+			inProgress, _ = sjson.SetBytes(inProgress, "output_index", search.OutputIndex)
+			out = append(out, emitEvent("response.web_search_call.in_progress", inProgress))
+		} else if typ == "web_search_tool_result" {
+			callID := cb.Get("tool_use_id").String()
+			searchIndex, ok := st.SearchCallByID[callID]
+			if !ok {
+				return noSSEOutput(out)
+			}
+			search := st.SearchCalls[searchIndex]
+			search.Completed = true
+
+			completed := []byte(`{"type":"response.web_search_call.completed","sequence_number":0,"item_id":"","output_index":0}`)
+			completed, _ = sjson.SetBytes(completed, "sequence_number", nextSeq())
+			completed, _ = sjson.SetBytes(completed, "item_id", fmt.Sprintf("ws_%s", callID))
+			completed, _ = sjson.SetBytes(completed, "output_index", search.OutputIndex)
+			out = append(out, emitEvent("response.web_search_call.completed", completed))
+
+			itemDone := []byte(`{"type":"response.output_item.done","sequence_number":0,"output_index":0,"item":{"id":"","type":"web_search_call","status":"completed","action":{"type":"search","query":""}}}`)
+			itemDone, _ = sjson.SetBytes(itemDone, "sequence_number", nextSeq())
+			itemDone, _ = sjson.SetBytes(itemDone, "output_index", search.OutputIndex)
+			itemDone, _ = sjson.SetBytes(itemDone, "item.id", fmt.Sprintf("ws_%s", callID))
+			itemDone, _ = sjson.SetBytes(itemDone, "item.action.query", gjson.Get(search.Query.String(), "query").String())
+			out = append(out, emitEvent("response.output_item.done", itemDone))
 		} else if typ == "thinking" {
 			out = append(out, st.finalizeAssistantMessage(nextSeq)...)
 			// start reasoning item
@@ -435,10 +487,16 @@ func ConvertClaudeResponseToOpenAIResponses(ctx context.Context, modelName strin
 				st.CurrentTextBuf.WriteString(t.String())
 			}
 		} else if dt == "input_json_delta" {
+			idx := int(root.Get("index").Int())
+			if search := st.SearchCalls[idx]; search != nil {
+				if partial := d.Get("partial_json"); partial.Exists() {
+					search.Query.WriteString(partial.String())
+				}
+				return noSSEOutput(out)
+			}
 			if !st.InFuncBlock || st.CurrentFCID == "" {
 				return [][]byte{}
 			}
-			idx := int(root.Get("index").Int())
 			if pj := d.Get("partial_json"); pj.Exists() {
 				if st.FuncArgsBuf[idx] == nil {
 					st.FuncArgsBuf[idx] = &strings.Builder{}
@@ -481,7 +539,13 @@ func ConvertClaudeResponseToOpenAIResponses(ctx context.Context, modelName strin
 		}
 	case "content_block_stop":
 		idx := int(root.Get("index").Int())
-		if st.InTextBlock {
+		if search := st.SearchCalls[idx]; search != nil && !search.Completed {
+			searching := []byte(`{"type":"response.web_search_call.searching","sequence_number":0,"item_id":"","output_index":0}`)
+			searching, _ = sjson.SetBytes(searching, "sequence_number", nextSeq())
+			searching, _ = sjson.SetBytes(searching, "item_id", fmt.Sprintf("ws_%s", search.ID))
+			searching, _ = sjson.SetBytes(searching, "output_index", search.OutputIndex)
+			out = append(out, emitEvent("response.web_search_call.searching", searching))
+		} else if st.InTextBlock {
 			st.InTextBlock = false
 		} else if st.InFuncBlock {
 			outputIndex := st.functionOutputIndex(idx)
@@ -650,6 +714,15 @@ func ConvertClaudeResponseToOpenAIResponses(ctx context.Context, modelName strin
 			item, _ = sjson.SetRawBytes(item, "summary.-1", summary)
 			outputsWrapper, _ = sjson.SetRawBytes(outputsWrapper, fmt.Sprintf("arr.%d", reasoning.OutputIndex), item)
 		}
+		for _, search := range st.SearchCalls {
+			if !search.Completed {
+				continue
+			}
+			item := []byte(`{"id":"","type":"web_search_call","status":"completed","action":{"type":"search","query":""}}`)
+			item, _ = sjson.SetBytes(item, "id", fmt.Sprintf("ws_%s", search.ID))
+			item, _ = sjson.SetBytes(item, "action.query", gjson.Get(search.Query.String(), "query").String())
+			outputsWrapper, _ = sjson.SetRawBytes(outputsWrapper, fmt.Sprintf("arr.%d", search.OutputIndex), item)
+		}
 		// assistant message items
 		for _, message := range st.MessageItems {
 			item := []byte(`{"id":"","type":"message","status":"completed","content":[{"type":"output_text","annotations":[],"logprobs":[],"text":""}],"role":"assistant"}`)
@@ -777,7 +850,14 @@ func ConvertClaudeResponseToOpenAIResponsesNonStream(_ context.Context, _ string
 		name string
 		args strings.Builder
 	}
+	type searchState struct {
+		id        string
+		query     strings.Builder
+		completed bool
+	}
 	toolCalls := make(map[int]*toolState)
+	searchCalls := make(map[string]*searchState)
+	searchCallsByIndex := make(map[int]*searchState)
 
 	// Walk through SSE chunks to fill state
 	for _, ch := range chunks {
@@ -811,6 +891,16 @@ func ConvertClaudeResponseToOpenAIResponsesNonStream(_ context.Context, _ string
 					toolCalls[idx].id = currentFCID
 					toolCalls[idx].name = name
 				}
+			case "server_tool_use":
+				if cb.Get("name").String() == "web_search" {
+					search := &searchState{id: cb.Get("id").String()}
+					searchCalls[search.id] = search
+					searchCallsByIndex[idx] = search
+				}
+			case "web_search_tool_result":
+				if search := searchCalls[cb.Get("tool_use_id").String()]; search != nil {
+					search.completed = true
+				}
 			case "thinking":
 				reasoningActive = true
 				reasoningItemID = fmt.Sprintf("rs_%s_%d", responseID, idx)
@@ -834,6 +924,10 @@ func ConvertClaudeResponseToOpenAIResponsesNonStream(_ context.Context, _ string
 			case "input_json_delta":
 				if pj := d.Get("partial_json"); pj.Exists() {
 					idx := int(root.Get("index").Int())
+					if search := searchCallsByIndex[idx]; search != nil {
+						search.query.WriteString(pj.String())
+						continue
+					}
 					if toolCalls[idx] == nil {
 						toolCalls[idx] = &toolState{}
 					}
@@ -950,6 +1044,15 @@ func ConvertClaudeResponseToOpenAIResponsesNonStream(_ context.Context, _ string
 		}
 		outputsWrapper, _ = sjson.SetRawBytes(outputsWrapper, "arr.-1", item)
 	}
+	for _, search := range searchCalls {
+		if !search.completed {
+			continue
+		}
+		item := []byte(`{"id":"","type":"web_search_call","status":"completed","action":{"type":"search","query":""}}`)
+		item, _ = sjson.SetBytes(item, "id", fmt.Sprintf("ws_%s", search.id))
+		item, _ = sjson.SetBytes(item, "action.query", gjson.Get(search.query.String(), "query").String())
+		outputsWrapper, _ = sjson.SetRawBytes(outputsWrapper, "arr.-1", item)
+	}
 	if currentMsgID != "" || textBuf.Len() > 0 {
 		item := []byte(`{"id":"","type":"message","status":"completed","content":[{"type":"output_text","annotations":[],"logprobs":[],"text":""}],"role":"assistant"}`)
 		item, _ = sjson.SetBytes(item, "id", currentMsgID)
diff --git a/internal/translator/claude/openai/responses/claude_openai-responses_response_test.go b/internal/translator/claude/openai/responses/claude_openai-responses_response_test.go
index 104a23c..589c3be 100644
--- a/internal/translator/claude/openai/responses/claude_openai-responses_response_test.go
+++ b/internal/translator/claude/openai/responses/claude_openai-responses_response_test.go
@@ -136,8 +136,8 @@ func TestConvertClaudeResponseToOpenAIResponses_AggregatesTextBlocksUntilMessage
 		}
 	}
 
-	if counts["response.output_item.added"] != 1 {
-		t.Fatalf("response.output_item.added count = %d, want 1", counts["response.output_item.added"])
+	if counts["response.output_item.added"] != 2 {
+		t.Fatalf("response.output_item.added count = %d, want 2", counts["response.output_item.added"])
 	}
 	if counts["response.content_part.added"] != 1 {
 		t.Fatalf("response.content_part.added count = %d, want 1", counts["response.content_part.added"])
@@ -148,8 +148,17 @@ func TestConvertClaudeResponseToOpenAIResponses_AggregatesTextBlocksUntilMessage
 	if counts["response.content_part.done"] != 1 {
 		t.Fatalf("response.content_part.done count = %d, want 1", counts["response.content_part.done"])
 	}
-	if counts["response.output_item.done"] != 1 {
-		t.Fatalf("response.output_item.done count = %d, want 1", counts["response.output_item.done"])
+	if counts["response.output_item.done"] != 2 {
+		t.Fatalf("response.output_item.done count = %d, want 2", counts["response.output_item.done"])
+	}
+	for _, event := range []string{
+		"response.web_search_call.in_progress",
+		"response.web_search_call.searching",
+		"response.web_search_call.completed",
+	} {
+		if counts[event] != 1 {
+			t.Fatalf("%s count = %d, want 1", event, counts[event])
+		}
 	}
 	if counts["response.function_call_arguments.delta"] != 0 {
 		t.Fatalf("response.function_call_arguments.delta count = %d, want 0", counts["response.function_call_arguments.delta"])
@@ -165,6 +174,12 @@ func TestConvertClaudeResponseToOpenAIResponses_AggregatesTextBlocksUntilMessage
 	if got := completed.Get("response.output.0.content.0.annotations.0.type").String(); got != "web_search_result_location" {
 		t.Fatalf("completed annotation type = %q", got)
 	}
+	if got := completed.Get("response.output.1.type").String(); got != "web_search_call" {
+		t.Fatalf("completed search output type = %q", got)
+	}
+	if got := completed.Get("response.output.1.action.query").String(); got != "Qwen3" {
+		t.Fatalf("completed search query = %q", got)
+	}
 }
 
 func TestConvertClaudeResponseToOpenAIResponses_FinalizesMessageBeforeFunctionCall(t *testing.T) {
@@ -296,24 +311,29 @@ func TestConvertClaudeResponseToOpenAIResponses_UsesContiguousIndicesForReasonin
 		case event == "response.output_item.added" || event == "response.output_item.done":
 			itemType = data.Get("item.type").String()
 			switch itemType {
-			case "reasoning":
+			case "web_search_call":
 				wantIndex = 0
-			case "message":
+			case "reasoning":
 				wantIndex = 1
-			case "function_call":
+			case "message":
 				wantIndex = 2
+			case "function_call":
+				wantIndex = 3
 			default:
 				continue
 			}
 		case strings.HasPrefix(event, "response.reasoning_"):
 			itemType = "reasoning"
+			wantIndex = 1
+		case strings.HasPrefix(event, "response.web_search_call."):
+			itemType = "web_search_call"
 			wantIndex = 0
 		case strings.HasPrefix(event, "response.output_text.") || strings.HasPrefix(event, "response.content_part."):
 			itemType = "message"
-			wantIndex = 1
+			wantIndex = 2
 		case strings.HasPrefix(event, "response.function_call_arguments."):
 			itemType = "function_call"
-			wantIndex = 2
+			wantIndex = 3
 		case event == "response.completed":
 			completed = data
 			continue
@@ -330,22 +350,22 @@ func TestConvertClaudeResponseToOpenAIResponses_UsesContiguousIndicesForReasonin
 		seen[itemType]++
 	}
 
-	for _, itemType := range []string{"reasoning", "message", "function_call"} {
+	for _, itemType := range []string{"web_search_call", "reasoning", "message", "function_call"} {
 		if seen[itemType] == 0 {
 			t.Fatalf("no indexed %s events observed", itemType)
 		}
 	}
-	if got := completed.Get("response.output.#").Int(); got != 3 {
-		t.Fatalf("completed output count = %d, want 3", got)
+	if got := completed.Get("response.output.#").Int(); got != 4 {
+		t.Fatalf("completed output count = %d, want 4", got)
 	}
-	for index, wantType := range []string{"reasoning", "message", "function_call"} {
+	for index, wantType := range []string{"web_search_call", "reasoning", "message", "function_call"} {
 		if got := completed.Get(fmt.Sprintf("response.output.%d.type", index)).String(); got != wantType {
 			t.Fatalf("completed output[%d].type = %q, want %q", index, got, wantType)
 		}
 	}
 }
 
-func TestConvertClaudeResponseToOpenAIResponses_HiddenServerToolsDoNotCreateOutputIndexGaps(t *testing.T) {
+func TestConvertClaudeResponseToOpenAIResponses_WebSearchUsesVisibleContiguousOutputIndex(t *testing.T) {
 	chunks := [][]byte{
 		[]byte(`data: {"type":"message_start","message":{"id":"msg_123","usage":{"input_tokens":1,"output_tokens":0}}}`),
 		[]byte(`data: {"type":"content_block_start","index":0,"content_block":{"type":"text","text":""}}`),
@@ -394,6 +414,11 @@ func TestConvertClaudeResponseToOpenAIResponses_HiddenServerToolsDoNotCreateOutp
 		case event == "response.output_item.added" && data.Get("item.type").String() == "function_call",
 			event == "response.output_item.done" && data.Get("item.type").String() == "function_call",
 			strings.HasPrefix(event, "response.function_call_arguments."):
+			if got := data.Get("output_index").Int(); got != 2 {
+				t.Fatalf("%s output_index = %d, want 2", event, got)
+			}
+		case (event == "response.output_item.added" || event == "response.output_item.done") && data.Get("item.type").String() == "web_search_call",
+			strings.HasPrefix(event, "response.web_search_call."):
 			if got := data.Get("output_index").Int(); got != 1 {
 				t.Fatalf("%s output_index = %d, want 1", event, got)
 			}
@@ -408,14 +433,17 @@ func TestConvertClaudeResponseToOpenAIResponses_HiddenServerToolsDoNotCreateOutp
 	if got := outputTextDone.Get("text").String(); got != "Searching. Found it." {
 		t.Fatalf("aggregated message text = %q, want %q", got, "Searching. Found it.")
 	}
-	if got := completed.Get("response.output.#").Int(); got != 2 {
-		t.Fatalf("completed output count = %d, want 2", got)
+	if got := completed.Get("response.output.#").Int(); got != 3 {
+		t.Fatalf("completed output count = %d, want 3", got)
 	}
 	if got := completed.Get("response.output.0.type").String(); got != "message" {
 		t.Fatalf("completed output[0].type = %q, want message", got)
 	}
-	if got := completed.Get("response.output.1.type").String(); got != "function_call" {
-		t.Fatalf("completed output[1].type = %q, want function_call", got)
+	if got := completed.Get("response.output.1.type").String(); got != "web_search_call" {
+		t.Fatalf("completed output[1].type = %q, want web_search_call", got)
+	}
+	if got := completed.Get("response.output.2.type").String(); got != "function_call" {
+		t.Fatalf("completed output[2].type = %q, want function_call", got)
 	}
 }
 
