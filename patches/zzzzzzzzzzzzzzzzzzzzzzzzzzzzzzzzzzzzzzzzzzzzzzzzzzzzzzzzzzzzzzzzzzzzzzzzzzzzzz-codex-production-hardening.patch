diff --git a/constant/madapi_codex.go b/constant/madapi_codex.go
index 981b4998..687cb2b3 100644
--- a/constant/madapi_codex.go
+++ b/constant/madapi_codex.go
@@ -1,9 +1,13 @@
 package constant
 
 const (
-	MadAPICodexDispatchTokenEnv = "MADAPI_CODEX_DISPATCH_TOKEN"
-	MadAPICodexDispatchHeader   = "X-MadAPI-Codex-Dispatch-Token"
-	MadAPICPADispatchURLEnv     = "MADAPI_CPA_DISPATCH_URL"
+	MadAPICodexDispatchTokenEnv   = "MADAPI_CODEX_DISPATCH_TOKEN"
+	MadAPICodexDispatchHeader     = "X-MadAPI-Codex-Dispatch-Token"
+	MadAPICPADispatchURLEnv       = "MADAPI_CPA_DISPATCH_URL"
+	MadAPICPACanaryDispatchURLEnv = "MADAPI_CPA_CANARY_DISPATCH_URL"
+	MadAPICPACanaryCockpitDispatchURLEnv = "MADAPI_CPA_CANARY_COCKPIT_DISPATCH_URL"
+	MadAPICodexCanaryHeader       = "X-MadAPI-Codex-Canary"
+	MadAPICodexCockpitHeader      = "X-MadAPI-Codex-Cockpit"
 )
 
 const ContextKeyMadAPICPAFrontCompactModel = "madapi_cpa_front_compact_model"
diff --git a/controller/codex_compat.go b/controller/codex_compat.go
index f9e74f6b..f01d5ab5 100644
--- a/controller/codex_compat.go
+++ b/controller/codex_compat.go
@@ -354,6 +354,12 @@ func newCodexInternalRequest(c *gin.Context, method string, path string, body io
 	if token := strings.TrimSpace(os.Getenv(appconstant.MadAPICodexDispatchTokenEnv)); token != "" {
 		req.Header.Set(appconstant.MadAPICodexDispatchHeader, token)
 	}
+	if strings.TrimSpace(c.GetHeader(appconstant.MadAPICodexCanaryHeader)) == "1" {
+		req.Header.Set(appconstant.MadAPICodexCanaryHeader, "1")
+	}
+	if strings.TrimSpace(c.GetHeader(appconstant.MadAPICodexCockpitHeader)) == "1" {
+		req.Header.Set(appconstant.MadAPICodexCockpitHeader, "1")
+	}
 	return req, nil
 }
 
diff --git a/controller/codex_compat_test.go b/controller/codex_compat_test.go
index c28ca579..3c7ce3fc 100644
--- a/controller/codex_compat_test.go
+++ b/controller/codex_compat_test.go
@@ -728,3 +728,16 @@ func TestCopyCodexTraceHeaders(t *testing.T) {
 
 	require.Equal(t, "req-inner-1", recorder.Header().Get("X-Oneapi-Request-Id"))
 }
+
+func TestCodexInternalRequestPropagatesCanaryCatalogHeaders(t *testing.T) {
+	recorder := httptest.NewRecorder()
+	c, _ := gin.CreateTestContext(recorder)
+	c.Request = httptest.NewRequest(http.MethodPost, "/codex/cockpit/v1/responses", nil)
+	c.Request.Header.Set("X-MadAPI-Codex-Canary", "1")
+	c.Request.Header.Set("X-MadAPI-Codex-Cockpit", "1")
+
+	request, err := newCodexInternalRequest(c, http.MethodPost, "/responses", nil)
+	require.NoError(t, err)
+	require.Equal(t, "1", request.Header.Get("X-MadAPI-Codex-Canary"))
+	require.Equal(t, "1", request.Header.Get("X-MadAPI-Codex-Cockpit"))
+}
diff --git a/controller/relay.go b/controller/relay.go
index 6efaf184..3aa05c0d 100644
--- a/controller/relay.go
+++ b/controller/relay.go
@@ -231,6 +231,7 @@ func Relay(c *gin.Context, relayFormat types.RelayFormat) {
 		relayInfo.LastError = newAPIError
 
 		processChannelError(c, *types.NewChannelError(channel.Id, channel.Type, channel.Name, channel.ChannelInfo.IsMultiKey, common.GetContextKeyString(c, constant.ContextKeyChannelKey), channel.GetAutoBan()), newAPIError)
+		retryParam.ExcludeChannel(channel.Id)
 
 		if !shouldRetry(c, newAPIError, common.RetryTimes-retryParam.GetRetry()) {
 			break
diff --git a/model/ability.go b/model/ability.go
index e67b2830..933f8a84 100644
--- a/model/ability.go
+++ b/model/ability.go
@@ -106,6 +106,10 @@ func getChannelQuery(group string, model string, retry int) (*gorm.DB, error) {
 }
 
 func GetChannel(group string, model string, retry int, requestPath string) (*Channel, error) {
+	return GetChannelExcluding(group, model, retry, requestPath, nil)
+}
+
+func GetChannelExcluding(group string, model string, retry int, requestPath string, excluded map[int]struct{}) (*Channel, error) {
 	var abilities []Ability
 
 	var err error = nil
@@ -122,6 +126,7 @@ func GetChannel(group string, model string, retry int, requestPath string) (*Cha
 		return nil, err
 	}
 	abilities = filterAbilitiesByRequestPathAndModel(abilities, requestPath, model)
+	abilities = preferUntriedAbilities(abilities, excluded)
 	channel := Channel{}
 	if len(abilities) > 0 {
 		// Randomly choose one
@@ -146,6 +151,22 @@ func GetChannel(group string, model string, retry int, requestPath string) (*Cha
 	return &channel, err
 }
 
+func preferUntriedAbilities(abilities []Ability, excluded map[int]struct{}) []Ability {
+	if len(abilities) == 0 || len(excluded) == 0 {
+		return abilities
+	}
+	untried := make([]Ability, 0, len(abilities))
+	for _, ability := range abilities {
+		if _, seen := excluded[ability.ChannelId]; !seen {
+			untried = append(untried, ability)
+		}
+	}
+	if len(untried) == 0 {
+		return abilities
+	}
+	return untried
+}
+
 // filterAbilitiesByRequestPathAndModel restricts candidates by request path and
 // model for the DB (non-memory-cache) selection path. Only Advanced Custom
 // (type 58) channels are path-checked: kept only when one of their routes matches
diff --git a/model/channel_cache.go b/model/channel_cache.go
index 81923017..0a281115 100644
--- a/model/channel_cache.go
+++ b/model/channel_cache.go
@@ -112,9 +112,13 @@ func SyncChannelCache(frequency int) {
 }
 
 func GetRandomSatisfiedChannel(group string, model string, retry int, requestPath string) (*Channel, error) {
+	return GetRandomSatisfiedChannelExcluding(group, model, retry, requestPath, nil)
+}
+
+func GetRandomSatisfiedChannelExcluding(group string, model string, retry int, requestPath string, excluded map[int]struct{}) (*Channel, error) {
 	// if memory cache is disabled, get channel directly from database
 	if !common.MemoryCacheEnabled {
-		return GetChannel(group, model, retry, requestPath)
+		return GetChannelExcluding(group, model, retry, requestPath, excluded)
 	}
 
 	channelSyncLock.RLock()
@@ -160,12 +164,10 @@ func GetRandomSatisfiedChannel(group string, model string, retry int, requestPat
 	targetPriority := int64(sortedUniquePriorities[retry])
 
 	// get the priority for the given retry number
-	var sumWeight = 0
 	var targetChannels []*Channel
 	for _, channelId := range channels {
 		if channel, ok := channelsIDM[channelId]; ok {
 			if channel.GetPriority() == targetPriority {
-				sumWeight += channel.GetWeight()
 				targetChannels = append(targetChannels, channel)
 			}
 		} else {
@@ -176,6 +178,11 @@ func GetRandomSatisfiedChannel(group string, model string, retry int, requestPat
 	if len(targetChannels) == 0 {
 		return nil, errors.New(fmt.Sprintf("no channel found, group: %s, model: %s, priority: %d", group, model, targetPriority))
 	}
+	targetChannels = preferUntriedChannels(targetChannels, excluded)
+	var sumWeight = 0
+	for _, channel := range targetChannels {
+		sumWeight += channel.GetWeight()
+	}
 
 	// smoothing factor and adjustment
 	smoothingFactor := 1
@@ -208,6 +215,22 @@ func GetRandomSatisfiedChannel(group string, model string, retry int, requestPat
 	return nil, errors.New("channel not found")
 }
 
+func preferUntriedChannels(channels []*Channel, excluded map[int]struct{}) []*Channel {
+	if len(channels) == 0 || len(excluded) == 0 {
+		return channels
+	}
+	untried := make([]*Channel, 0, len(channels))
+	for _, channel := range channels {
+		if _, seen := excluded[channel.Id]; !seen {
+			untried = append(untried, channel)
+		}
+	}
+	if len(untried) == 0 {
+		return channels
+	}
+	return untried
+}
+
 // filterChannelsByRequestPathAndModel restricts candidates by request path and
 // model. Only Advanced Custom (type 58) channels are path-checked: they are kept
 // only when one of their configured routes matches requestPath and model. All
diff --git a/model/channel_retry_madapi_test.go b/model/channel_retry_madapi_test.go
new file mode 100644
index 00000000..2e1f039a
--- /dev/null
+++ b/model/channel_retry_madapi_test.go
@@ -0,0 +1,27 @@
+package model
+
+import "testing"
+
+func TestPreferUntriedChannelsUsesWeightsWithoutRepeating(t *testing.T) {
+	channels := []*Channel{{Id: 18}, {Id: 50}, {Id: 63}}
+	got := preferUntriedChannels(channels, map[int]struct{}{50: {}})
+	if len(got) != 2 || got[0].Id != 18 || got[1].Id != 63 {
+		t.Fatalf("untried channels = %#v, want 18 and 63", got)
+	}
+	got = preferUntriedChannels(channels, map[int]struct{}{18: {}, 50: {}, 63: {}})
+	if len(got) != len(channels) {
+		t.Fatalf("all channels exhausted = %d candidates, want %d", len(got), len(channels))
+	}
+}
+
+func TestPreferUntriedAbilitiesFallsBackAfterExhaustion(t *testing.T) {
+	abilities := []Ability{{ChannelId: 18}, {ChannelId: 50}}
+	got := preferUntriedAbilities(abilities, map[int]struct{}{18: {}})
+	if len(got) != 1 || got[0].ChannelId != 50 {
+		t.Fatalf("untried abilities = %#v, want channel 50", got)
+	}
+	got = preferUntriedAbilities(abilities, map[int]struct{}{18: {}, 50: {}})
+	if len(got) != len(abilities) {
+		t.Fatalf("all abilities exhausted = %d candidates, want %d", len(got), len(abilities))
+	}
+}
diff --git a/relay/cpa_native_codex.go b/relay/cpa_native_codex.go
index 540ade42..7caece48 100644
--- a/relay/cpa_native_codex.go
+++ b/relay/cpa_native_codex.go
@@ -26,6 +26,8 @@ import (
 )
 
 const defaultMadAPICPADispatchURL = "http://cpa-codex:8317/internal/madapi/codex/execute"
+const defaultMadAPICPACanaryDispatchURL = "http://cpa-codex-canary:8317/internal/madapi/codex/execute"
+const defaultMadAPICPACanaryCockpitDispatchURL = "http://cpa-codex-cockpit-canary:8317/internal/madapi/codex/execute"
 
 const (
 	madAPICodexProtocolOpenAIResponses   = "openai_responses"
@@ -94,6 +96,18 @@ func relayMadAPICodexNative(c *gin.Context, info *relaycommon.RelayInfo) *types.
 	if dispatchURL == "" {
 		dispatchURL = defaultMadAPICPADispatchURL
 	}
+	if strings.TrimSpace(c.GetHeader(appconstant.MadAPICodexCanaryHeader)) == "1" {
+		dispatchURL = strings.TrimSpace(os.Getenv(appconstant.MadAPICPACanaryDispatchURLEnv))
+		if strings.TrimSpace(c.GetHeader(appconstant.MadAPICodexCockpitHeader)) == "1" {
+			dispatchURL = strings.TrimSpace(os.Getenv(appconstant.MadAPICPACanaryCockpitDispatchURLEnv))
+			if dispatchURL == "" {
+				dispatchURL = defaultMadAPICPACanaryCockpitDispatchURL
+			}
+		}
+		if dispatchURL == "" {
+			dispatchURL = defaultMadAPICPACanaryDispatchURL
+		}
+	}
 	req, err := http.NewRequestWithContext(c.Request.Context(), http.MethodPost, dispatchURL, bytes.NewReader(requestBody))
 	if err != nil {
 		return types.NewError(err, types.ErrorCodeDoRequestFailed)
@@ -146,8 +160,8 @@ func relayMadAPICodexNative(c *gin.Context, info *relaycommon.RelayInfo) *types.
 
 func madAPICodexFirstEventTimeoutForModel(model string) time.Duration {
 	switch strings.ToLower(strings.TrimSpace(model)) {
-	case "gpt-5.6-terra", "gpt-5.6-sol", "gpt-5.6-luna":
-		return 30 * time.Second
+	case "gpt-5.5", "gpt-5.6-terra", "gpt-5.6-sol", "gpt-5.6-luna":
+		return 60 * time.Second
 	default:
 		return 0
 	}
@@ -164,7 +178,7 @@ func madAPICPADispatchClientForFirstEvent(model string) *http.Client {
 	}
 	client := *madAPICPADispatchClient
 	attemptTransport := transport.Clone()
-	attemptTransport.ResponseHeaderTimeout = timeout
+	attemptTransport.ResponseHeaderTimeout = timeout + 10*time.Second
 	client.Transport = attemptTransport
 	return &client
 }
@@ -180,10 +194,9 @@ func madAPICodexWireProtocol(channelType int, model string) string {
 	case appconstant.ChannelTypeXai:
 		return madAPICodexProtocolXAIResponses
 	case appconstant.ChannelTypeOpenAI:
-		if strings.HasPrefix(strings.ToLower(strings.TrimSpace(model)), "gpt-") {
-			return madAPICodexProtocolOpenAIResponses
-		}
+		return madAPICodexProtocolOpenAIChat
 	}
+	_ = model
 	return madAPICodexProtocolOpenAIChat
 }
 
diff --git a/relay/cpa_native_codex_test.go b/relay/cpa_native_codex_test.go
index 6f49818b..fd710a56 100644
--- a/relay/cpa_native_codex_test.go
+++ b/relay/cpa_native_codex_test.go
@@ -33,9 +33,9 @@ func TestMadAPICodexNativeRequestRequiresLoopbackAndToken(t *testing.T) {
 }
 
 func TestMadAPICodexFirstEventDeadlineOnlyAppliesToMeasuredGPTModels(t *testing.T) {
-	for _, model := range []string{"gpt-5.6-terra", "gpt-5.6-sol", "gpt-5.6-luna"} {
-		if timeout := madAPICodexFirstEventTimeoutForModel(model); timeout != 30*time.Second {
-			t.Fatalf("model %s timeout = %s, want 30 seconds", model, timeout)
+	for _, model := range []string{"gpt-5.5", "gpt-5.6-terra", "gpt-5.6-sol", "gpt-5.6-luna"} {
+		if timeout := madAPICodexFirstEventTimeoutForModel(model); timeout != 60*time.Second {
+			t.Fatalf("model %s timeout = %s, want 60 seconds", model, timeout)
 		}
 	}
 	if timeout := madAPICodexFirstEventTimeoutForModel("deepseek-v4-pro"); timeout != 0 {
@@ -43,8 +43,8 @@ func TestMadAPICodexFirstEventDeadlineOnlyAppliesToMeasuredGPTModels(t *testing.
 	}
 	client := madAPICPADispatchClientForFirstEvent("gpt-5.6-sol")
 	transport, ok := client.Transport.(*http.Transport)
-	if !ok || transport.ResponseHeaderTimeout != 30*time.Second {
-		t.Fatalf("first-event transport timeout = %#v, want 30 seconds", client.Transport)
+	if !ok || transport.ResponseHeaderTimeout != 70*time.Second {
+		t.Fatalf("first-event transport timeout = %#v, want 70 seconds", client.Transport)
 	}
 	if madAPICPADispatchClientForFirstEvent("deepseek-v4-pro") != madAPICPADispatchClient {
 		t.Fatal("unmeasured model must keep the existing dispatch client")
@@ -103,8 +103,8 @@ func TestMadAPICodexDispatchPayloadPreservesSelectedChannel(t *testing.T) {
 	if err != nil {
 		t.Fatal(err)
 	}
-	if payload.FirstEventTimeoutSeconds != 30 {
-		t.Fatalf("measured GPT timeout = %d, want 30", payload.FirstEventTimeoutSeconds)
+	if payload.FirstEventTimeoutSeconds != 60 {
+		t.Fatalf("measured GPT timeout = %d, want 60", payload.FirstEventTimeoutSeconds)
 	}
 }
 
@@ -117,7 +117,7 @@ func TestMadAPICodexWireProtocolUsesModelAndChannelContract(t *testing.T) {
 		protocol    string
 		wantBaseURL string
 	}{
-		{"OpenAI GPT uses Responses", appconstant.ChannelTypeOpenAI, "gpt-5.6-sol", "https://gpt.example", madAPICodexProtocolOpenAIResponses, "https://gpt.example/v1"},
+		{"OpenAI GPT stays compatible", appconstant.ChannelTypeOpenAI, "gpt-5.6-sol", "https://gpt.example", madAPICodexProtocolOpenAIChat, "https://gpt.example/v1"},
 		{"OpenAI Claude stays compatible", appconstant.ChannelTypeOpenAI, "claude-opus-5", "https://compat.example/v1/", madAPICodexProtocolOpenAIChat, "https://compat.example/v1"},
 		{"native Claude uses Messages", appconstant.ChannelTypeAnthropic, "claude-opus-5", "https://claude.example/v1", madAPICodexProtocolAnthropicMessages, "https://claude.example"},
 		{"native Gemini uses generateContent", appconstant.ChannelTypeGemini, "gemini-3.6-flash", "https://gemini.example/v1beta", madAPICodexProtocolGeminiGenerate, "https://gemini.example"},
@@ -145,10 +145,10 @@ func TestMadAPICodexCurrentCatalogUsesValidatedProtocols(t *testing.T) {
 		channelType int
 		protocol    string
 	}{
-		{"gpt-5.6-sol", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIResponses},
-		{"gpt-5.6-terra", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIResponses},
-		{"gpt-5.6-luna", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIResponses},
-		{"gpt-5.5", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIResponses},
+		{"gpt-5.6-sol", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIChat},
+		{"gpt-5.6-terra", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIChat},
+		{"gpt-5.6-luna", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIChat},
+		{"gpt-5.5", appconstant.ChannelTypeOpenAI, madAPICodexProtocolOpenAIChat},
 		{"claude-fable-5", appconstant.ChannelTypeAnthropic, madAPICodexProtocolAnthropicMessages},
 		{"claude-opus-4-8", appconstant.ChannelTypeAnthropic, madAPICodexProtocolAnthropicMessages},
 		{"claude-opus-5", appconstant.ChannelTypeAnthropic, madAPICodexProtocolAnthropicMessages},
diff --git a/service/channel_select.go b/service/channel_select.go
index 24c4e252..14b0abcd 100644
--- a/service/channel_select.go
+++ b/service/channel_select.go
@@ -12,12 +12,23 @@ import (
 )
 
 type RetryParam struct {
-	Ctx          *gin.Context
-	TokenGroup   string
-	ModelName    string
-	RequestPath  string
-	Retry        *int
-	resetNextTry bool
+	Ctx                *gin.Context
+	TokenGroup         string
+	ModelName          string
+	RequestPath        string
+	Retry              *int
+	ExcludedChannelIDs map[int]struct{}
+	resetNextTry       bool
+}
+
+func (p *RetryParam) ExcludeChannel(channelID int) {
+	if channelID <= 0 {
+		return
+	}
+	if p.ExcludedChannelIDs == nil {
+		p.ExcludedChannelIDs = make(map[int]struct{})
+	}
+	p.ExcludedChannelIDs[channelID] = struct{}{}
 }
 
 func (p *RetryParam) GetRetry() int {
@@ -116,7 +127,7 @@ func CacheGetRandomSatisfiedChannel(param *RetryParam) (*model.Channel, string,
 			}
 			logger.LogDebug(param.Ctx, "Auto selecting group: %s, priorityRetry: %d", autoGroup, priorityRetry)
 
-			channel, _ = model.GetRandomSatisfiedChannel(autoGroup, param.ModelName, priorityRetry, param.RequestPath)
+			channel, _ = model.GetRandomSatisfiedChannelExcluding(autoGroup, param.ModelName, priorityRetry, param.RequestPath, param.ExcludedChannelIDs)
 			if channel == nil {
 				// Current group has no available channel for this model, try next group
 				// 当前分组没有该模型的可用渠道，尝试下一个分组
@@ -154,7 +165,7 @@ func CacheGetRandomSatisfiedChannel(param *RetryParam) (*model.Channel, string,
 			break
 		}
 	} else {
-		channel, err = model.GetRandomSatisfiedChannel(param.TokenGroup, param.ModelName, param.GetRetry(), param.RequestPath)
+		channel, err = model.GetRandomSatisfiedChannelExcluding(param.TokenGroup, param.ModelName, param.GetRetry(), param.RequestPath, param.ExcludedChannelIDs)
 		if err != nil {
 			return nil, param.TokenGroup, err
 		}
diff --git a/service/channel_select_madapi_test.go b/service/channel_select_madapi_test.go
new file mode 100644
index 00000000..a10b64f8
--- /dev/null
+++ b/service/channel_select_madapi_test.go
@@ -0,0 +1,16 @@
+package service
+
+import "testing"
+
+func TestRetryParamTracksFailedChannels(t *testing.T) {
+	param := &RetryParam{}
+	param.ExcludeChannel(63)
+	param.ExcludeChannel(50)
+	param.ExcludeChannel(63)
+	if len(param.ExcludedChannelIDs) != 2 {
+		t.Fatalf("excluded channels = %v, want two unique channels", param.ExcludedChannelIDs)
+	}
+	if _, ok := param.ExcludedChannelIDs[63]; !ok {
+		t.Fatal("channel 63 was not retained")
+	}
+}
